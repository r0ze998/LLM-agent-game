# Server
PORT=3001
CORS_ORIGIN=http://localhost:3000

# LLM (required for agent AI)
ANTHROPIC_API_KEY=sk-ant-xxx

# Database (optional — game runs in-memory by default)
# DATABASE_URL=postgresql://localhost:5432/murasato
